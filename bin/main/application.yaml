# yaml => 설정 파일
spring:
   datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/dw_study?useUnicode=true&charaterEncoding=utf-8&serverTimezone=UTC
      username: root
      password: qwer
      initialization-mode: never
      schema: classpath:init.sql

mybatis:
   mapper-locations: classpath:/sqlmapper/sqlmapper_*.xml
   type-aliases-package: com.portfolio.dyport.vo
   
pagehelper:
## helper-dialect: mysql : 사용하는 데이터 베이스
 helper-dialect: mysql
## reasonable: true (0페이지 1페이지,13페이지 까지 있는데 14페이지 요청이 들어오면 13페이지로 리턴해줌)  
 reasonable: true
 
google:
 recaptcha:
 secret-key: 6Le3nIwiAAAAAC2BqQdAF-YFZwB_iKuFfjN8P8si